#!/bin/bash

set -euo pipefail

DASHBOARD_SRC=($(find -type f -path "./src/*" -name "*.jsonnet"))
DASHBOARD_DEST=()

function loop_src_dir() {
    for i in ${SRC_ARRAY[@]}
    do
        
    done
}

loop_src_dir

